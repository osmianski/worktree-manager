{
    "name": "osmianski/worktree-manager",
    "description": "Need to maintain multiple working branches simultaneously - whether for long-term parallel development or running AI agents in separate worktrees? Setting up each environment is tedious. This tool creates worktrees with fully working environments in seconds.",
    "type": "library",
    "license": "MIT",
    "require": {
        "php": "^8.1",
        "ext-sockets": "*",
        "symfony/console": "^6.0|^7.0",
        "symfony/yaml": "^6.0|^7.0",
        "symfony/process": "^6.0|^7.0"
    },
    "autoload": {
        "psr-4": {
            "Osmianski\\WorktreeManager\\": "src/"
        },
        "files": [
            "src/functions.php"
        ]
    },
    "bin": ["bin/worktree"]
}
